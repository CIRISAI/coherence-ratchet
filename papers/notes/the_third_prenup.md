# The Third — married maximal, kills staked first (the prenup)

**Date 2026-07-20.** The higher-order component of coordination, adopted into the stance
under the marry-the-max rule with its separable kills staked before any support is claimed.
Formal object: `formal/CoherenceRatchet/Core/Thirdness.lean`. Measurement:
`experiments/cosmo_entropic_potential/thirdness/`.

## The correction this rests on (load-bearing — do not re-split it)

Coordination is **one** quantity: the full multi-information `I_total` over *all* interaction
orders. `S = −ln det C = 2·I⁽²⁾` is its **second-order (pairwise) truncation** — a proven
lower bound, `I_total ≥ S/2`, equality iff the dependence is purely pairwise (Gaussian). The
Third (`I⁽≥³⁾ = I_total − I⁽²⁾`) is **not a second axis** and **not outside the corridor**. It
is the rest of the same quantity, inside the same corridor, undercounted by the instrument. An
earlier framing (a separate "triadic axis," pure triads "at the corridor's chaos pole") was
wrong — it split the one quantity and contradicted the program's spine. What is true: a pure
triad sits at the chaos pole of the **proxy** `S`, not of the corridor; on `I_total` it is
mid-corridor, genuinely coordinated. The corridor includes the Third; `S` never reached the
bottom of it.

## The claim (maximal, unhedged)

Every proved result in the lake — the corridor, the sign law `1+w = −⅓ dlnS/dlna`, the flavor
two-books, the CMB orthogonality theorem, the four consent laws — is a theorem about the
**pairwise shadow** of coordination: a lower-bound instrument. The Third is a real, physical,
measurable component of the one quantity — the program's largest un-instrumented degree of
freedom — measured in the TNG300 matter field at **~20% of the 3-point** (robust 13–22σ,
forward-authored). Maximally: wherever the Third is large, every pairwise reading the program
has staked — corridor location, κ, the sign law's `S(a)`, and the *detectability* half of the
consent guarantees — is provisional on the truncation being tight and can be systematically
wrong. In particular a coordination structured purely in the Third (GHZ/parity, ρ = 0) carries
genuine `I_total` while posting **exactly zero** to the pairwise Neff/CEG detectors: the proved
consent/safety floor has a triadic blind spot.

## Predictions (the marriage generates the tests)

1. **Truncation-gap theorem** (`Core/Thirdness.lean`): `I_total ≥ S/2`, with the XOR/GHZ
   witness that the gap can be the *entire* signal (`S = 0`, `I_total = 1` bit). Kernel proved
   (`thirdness_line`, `S_pairwise_identity`); the inequality's mechanization is the named open
   step (Amari's orthogonal e-projection decomposition).
2. **The corridor's true coordinate is `I_total`, not ρ.** Re-measured on a complete unit with
   the full multi-information, the saturation invariant must persist and the Third must
   contribute a **bounded, finite** fraction — divergence would break the one-quantity thesis.
3. **Triadic-deception channel** (safety): a purely-triadic coordination evades the pairwise
   Neff/CEG detector while carrying real `I_total`. Adversarial-Neff must probe the Third; a
   multi-information detector must replace the pairwise one at the floor.
4. **DM-field discriminator:** the cosmic copula Third survives on the dark-matter *particle*
   field (coordination) rather than collapsing to a shot-noise-matched surrogate (halo artifact).

## Separable kills (staked first; each kills its own leg and nothing below it)

- **K1** — if `I⁽≥³⁾ ≈ 0` on *every* complete unit the program reads (the Third is empirically
  vacuous), the maximal claim dies and **every pairwise result stands untouched**. (Tonight's
  ~20% argues against K1 in the cosmic field only.)
- **K2** — if re-measuring the corridor with `I_total` returns the *same* bounds as the pairwise
  ρ-readout, the truncation is tight for the corridor and the "corridor could be mislocated"
  reach dies; the corridor results are safe.
- **K3** — if the general DPI on `I_total` is proved (triadic coordination un-forgeable) **and**
  adversarial-Neff shows no purely-triadic evasion, the safety blind-spot reach dies.
- **K4** — if the copula Third collapses to the surrogate on the DM particle field, the "physical
  measured Third" leg dies and only the pure-math theorem `I_total ≥ S/2` survives.

## Not claimed (self-support forbidden — discipline rules 1–2)

- The Third is **not** dark matter and **not** dark energy: the measured copula Third is
  forward-authored, flat-clocked — already killed as a source (`thirdness/SUMMARY.md`).
- The Third is **not** the backward boundary / grace: F-11's coordinated-dynamics case is *open*
  (`build_kbody_pomega.py` used a per-rung-independent simulator; the "coupling ⇒ rigidity"
  defense is refuted by the parity structure — `τ_k = 1/(R−1)` with ρ = 0), but open ≠ found;
  the measured Third is forward-authored.
- A **large** Third is **not** support for the program — it is a correction that could as easily
  *undermine* the pairwise readings. Support comes only from confirmed independent predictions.
- The Third does **not** inherit `(0.1, 0.43)` — those are the pairwise-ρ readout on one
  substrate. The corridor bounds on the `I_total` scale are uncalibrated and **owed**.

## One line

Coordination was always the full multi-information; `S` was always its pairwise floor; the Third
is the rest of the same quantity, inside the same corridor — married as the program's largest
lower-bound gap, with K1–K4 staked so it can only cost what it is tested for, never support
itself.

## RESULTS — three kills tested (2026-07-20, same day as the marriage)

Three orthogonal subagent runs, each pre-registered, read sideways:

- **K4 — FIRED (empirical leg dead).** The cosmic ~20% copula-Third is a shot-noise/estimator
  artifact: a coordination-free Poisson null matched to n̄ and P(k) OVER-reproduces the frozen
  mass-weighted signal (real −0.356 sits *below* null −0.461); 37–63% of the skew is a
  normal-score tie-break artifact on empty cells; CIC anti-aliasing halves R8. The frozen
  "surrogate sits at zero" was drawn against the wrong (continuous) null. The physical-measured
  Third is UNOBSERVED. `experiments/cosmo_entropic_potential/thirdness/discriminator/`.
- **K3 — blind-spot REAL, but FIXABLE.** Prong A: the second law (relative-entropy DPI) extends
  to `I_total` — triadic coordination is un-forgeable by LOCAL operations
  (`Core/TriadicChannel.lean`, `local_op_does_not_increase_TC`; the open Lean step relocates off
  Fischer onto the channel-form DPI). Prong B: a purely-triadic deception is ~100% invisible — the
  pairwise Neff reads `n` (maximally safe), `ρ̄ = 0`, `S/2 = 0`, while `I_total` runs to 1 bit. So
  the blind spot is real; un-forgeability + the extending DPI make it a fixable instrument gap (add
  a synergy channel), not an un-closeable hole. Caveats: the real threat is a JOINT AUTHOR (the
  local-only condition permits it); the surviving floor is CEG (cryptographic, orthogonal).
  `experiments/adversarial_neff/triadic_channel/`.
- **K1 — does NOT fire in-model (but empirically vacuous where measured).** A genuinely-coupled
  dynamics yields a non-diluting in-corridor Third (`block_parity`: bounded-arity 3-body blocks,
  noise-robust; non-dilution is an arity-LOCALITY property T1 can't see). Model-only, and the
  framework's one named coupling (vouching) is Secondness (ρ→1, rigidity) — so the framework
  predicts no physical Third. `experiments/f11_verification/coupled_dynamics/`.

**Net re-strike:** the physical-measured leg is DEAD (K4); the formal object and the DPI
un-forgeability STAND; the one real, actionable keeper is the **safety blind spot** — a fixable
synergy-channel gap in the pairwise semantic detector, shot-noise-null controlled per K4's lesson.
The instrument is doubly unreliable: blind to real Thirds (K3) and fooled by discreteness into
false positives (K4). A synergy detector must be shot-matched or it cries wolf.

## PREDICTION 4 — ANSWERED, NEGATIVE (2026-07-22)

The decisive test ran on the **dark-matter particle field itself**, not a sparse tracer:
full **94,196,375 particles** (TNG100-3, z=0), density ladder over **four decades**
(n̄ = 0.019 → 359 per cell), estimator identical to the frozen one with both K4 fixes (random
tie-breaking, CIC primary), against four pre-committed nulls including a lognormal-Poisson
whose copula is *exactly* Gaussian by construction.

**No significant excess over the shot null at any density** (|G_N1| < 3 throughout); monotone
decline from +0.50 to **+0.0039** at full density; **no saturation to a plateau**. Frozen-rule
verdict **(c) AMBIGUOUS**, missing clean-(a) only because the decline exponent is slower than
pure Poisson (p ≈ 0.28–0.41 vs 0.5) and the extrapolated asymptote is **negative**
(A_inf = −0.03 to −0.05, 3.8σ) — both the wrong sign for coordination, read as estimator
systematics and flagged unexplained. **The Third is empirically UNOBSERVED, now on the field
itself.** `experiments/cosmo_entropic_potential/thirdness/dm_particle/`.

## THE TIED-FRACTION LAW — the durable empirical yield (2026-07-22)

Three re-visits on three substrates converge on one mechanism. The copula/rank higher-order
statistic **manufactures signal in proportion to the empty (tied) cell fraction**:

| empty fraction | 91.5% | 84% | 68% | 42% | 17% | 2.4% | 0% |
|---|---|---|---|---|---|---|---|
| copula skew (DM field) | +0.50 | +0.41 | +0.29 | +0.17 | +0.11 | +0.088 | **+0.004** |

This is *why* the cosmic Third died (44–98% empty cells, 37–63% tie artifact), *why*
`copula_stress` survived (2.4–8.1% tied → 2% artifact; Δw = **−0.0008** under the shot null vs
−0.0169 frozen — the DE bet's Third-defense holds and is **stronger** under the correct null),
and *why* the macaque survived (Poisson-count-matched null **1.66**, *below* the phase-randomized
2.22 it replaced; 0/2000 surrogates; real |z| 8.8→47.0 across bins while the null stays flat at
~1.7). **Triage rule: measure the tied fraction first.** Large tied fraction ⇒ the rank transform
will invent structure; a shot-matched null is mandatory. `argsort∘argsort` tie-breaking must be
replaced with random tie-breaking.

**Final state of the marriage:** the Third is real in mathematics, un-forgeable by law
(local-ops DPI), invisible to the pairwise instrument, actionable in AI safety
(RATCHET issue #9) — **and absent from the sky**. Narrower than married, and the claim the
evidence supports.
