# Rung-1 mixing test — registration (minimal, one claim)

**Date 2026-07-10, written before any computation.** CKM/PMNS values are known targets, so
per the discovery/confirmation boundary exactly ONE claim is registered; everything else in
this experiment is discovery-mode (exhaustive reporting, sideways pass, no kill-scoring).

**Registered claim (staked in `papers/notes/sm_escalator_map.md` §4 before this experiment
existed):** under the ledger functional applied to the generation-coupling matrix
`P(i,j) = |V_ij|²/3`, the **PMNS matrix is Haar-typical** — its coordination value lies
within the central 90% of the Haar-random U(3) ensemble — while the **CKM matrix is extreme
toward the aligned pole**, beyond the 99.9th Haar percentile. The content is the
**asymmetry**: lepton coupling readable as structureless/anarchic on the ledger's own
functional, quark coupling conceded to the marginal (mass-hierarchy/GST) sector.

**Kill:** PMNS falls outside the Haar bulk (anarchy fails on the ledger's functional → lepton
mixing is also mass-ratio-sourced → Rung 1 collapses into the Rung-7 marginal no-go), or CKM
is Haar-typical (no asymmetry → nothing read).

Primary functional: mutual information of `P(i,j)` (the ledger's native object on a
categorical coupling; `S = −ln det C` on one-hot encoding reported alongside). Parameter
sources: CKM PDG 2024 (s12 = 0.22500, s23 = 0.04185, s13 = 0.00369, δ = 1.144); PMNS
NuFit-6.0 NO (sin²θ12 = 0.308, sin²θ13 = 0.0220, sin²θ23 = 0.470 and 0.561 octant variants,
δ = −1.98 rad), arXiv:2410.05380. Everything downstream of this paragraph is discovery.
