# Dark-matter mass from the ledger — KILL, and the kill is structural

**Date 2026-07-10.** Sweep question (neutral brief, run to hit): does the 1+2 synthesis —
the fermionic ledger (exclusion caps rigidity at ln2/mode) linked to the DE scale
ρ_Λ = κ·S — predict a dark-matter particle mass via the Tremaine-Gunn phase-space bound
(Q_max ∝ m⁴)? HIT = a derivable, non-arbitrary link landing in the allowed [~0.3, ~10] keV
window. Data: `results.json`, script `dark_sector_mass.py`.

## Verdict: KILL — kills (a) and (c) both fired

**Kill (a): no non-arbitrary link exists, and the reason is structural, not numerical.**
The ledger emits only **dimensionless** numbers — ln2 per mode, the corridor ρ ∈ (0.1, 0.43),
k_eff ≤ 10, dlnS/dlna. Bridging ρ_Λ to Q_max(m) requires an inserted dimensionful scale the
ledger does not contain — this is exactly the undetermined κ of the maintenance-w(z) note
(7a), met again from the matter side. Across the candidate bridges swept, the "predicted"
mass spans **41 orders of magnitude** (meV to 10⁸ eV to 10⁻³³ eV) — the signature of a
free scale, not a derivation.

**Kill (c): the only route using ρ_Λ alone reproduces prior art.** ρ_Λ^{1/4} ≈ 2.24 meV is
the known DE↔neutrino-mass numerical coincidence — six orders below the keV window and zero
new ledger content. And the only route that *lands* in-window (m ≳ 0.56 keV, Segue-1-like)
is standard Tremaine-Gunn with the dwarf velocity dispersion inserted by hand — pure standard
physics; ρ_Λ and the ledger never enter.

One route (E_mode = m, so ρ_Λ = ln2·n·m = ln2·ρ_DM) has the mass cancel entirely, and its
parameter-free density identity misses by 3.7× — a clean secondary no.

## The sideways read (what the kill teaches, not just what it denies)

The framework is **scale-free by construction**: the coordination functional is a copula
object (amplitude-blind by theorem), so every prediction it can natively make is
dimensionless — signs, ratios, log-derivatives, saturation ceilings. That is *why* the
sign law 1 + w = −⅓·dlnS/dlna is parameter-free (κ cancels) and *why* no mass can come out:
the same theorem clause powers both. Any future dimensionful prediction (a mass, a κ, a unit
scale) must come from **marrying an external scale** — and the program already has exactly
one such marriage pending: the retention/galaxy-host scale (~7×10¹¹ M⊙/h) as the physical
unit input. The mass question and the unit-scale question are the same question; this sweep
closes the ρ_Λ-side door and leaves the retention-anchor door as the only live one.

Standard inputs verified against literature: TG bound reproduces m ≳ 100–560 eV
(Boyarsky-class); ρ_Λ^{1/4} reproduces the textbook 2.24 meV; the [0.3, 10] keV window
(TG lower / Lyman-α upper) fetched, not rederived.

*(Computation by the dm-mass agent; summary by the orchestrator from the agent's executed
output.)*
