# Thirdness of the cosmic halo field — the blind run landed (2026-07-20)

> **SUPERSEDED — K4 FIRED (2026-07-20, `discriminator/`).** The detection reported below is a
> shot-noise / estimator artifact, NOT genuine matter-field coordination: a coordination-free
> Poisson null matched to n̄ and P(k) over-reproduces it; 37–63% is a normal-score tie-break
> artifact; CIC anti-aliasing halves R8. The "surrogate sits at zero" claim below was drawn
> against the wrong (continuous) null. The physical-measured Third is UNOBSERVED. Text kept for
> the record; see `discriminator/` and `papers/notes/the_third_prenup.md`.

**The uncashed measurement, run blind (method frozen in `DECISIONS.md` before any number was
seen). The question: is the ledger's ≥3-order blind spot empty, and does it have a history?**
Real TNG300-1 halo field, 26 snapshots z=3→0, phase-randomized surrogate null (identical power
spectrum ⇒ identical `S`, zero connected ≥3-point ⇒ any gap is exactly what `S` cannot see).

## Result 1 — the copula (amplitude-blind) Third is NOT empty. Detection.

The decisive estimator — skewness of the **normal-scored** field (marginals forced to Gaussian,
so all amplitude/marginal structure removed; only joint/shape dependence survives) — is robustly
nonzero at small scales:

| smoothing R | mean copula-skew | sign across 26 snaps | per-snap z (mean / min) | copula/raw ratio |
|---|---|---|---|---|
| 4 Mpc/h  | **−0.538** | 26/26 negative | −3.9 / −6.5 | 0.21 |
| 8 Mpc/h  | −0.356 | 26/26 negative | −1.6 / −3.0 | 0.21 |
| 16 Mpc/h | −0.085 | 25/26 negative | −0.2 / −0.8 | 0.05 |

The surrogate null sits at zero (mean ≈ −0.01 to −0.02) — the normal-score+smooth pipeline
manufactures no skewness, so this is not an estimator artifact; the real field's phase structure
(its ≥3-point correlations) produces it and a pairwise-matched Gaussian-copula field does not.
**~20% of the raw 3-point signal survives amplitude removal and is genuine, dimensionless,
shape-level three-way coordination — on the same footing as `S`, and `S` is provably blind to it.**

**This falsifies the program's own expectation.** `copula_invariance_remark.md` asked "does
coordination carried purely above second order gravitate? The entropic reading says NO," and the
stance reads higher-order structure as "off-books, posts no κS." At small scales that is wrong:
the shape-level Third is occupied, not vacuous. The honest revision: **empty at large scales
(R≳16 Mpc/h — the field is pairwise-complete there, the DE-leg's large-scale books are safe),
~20%-occupied at small (nonlinear) scales.**

## Result 2 — CORRECTED by the 27-sub-cube jackknife: the history is NOT flat.

The single-box read suggested "flat in time" (`corr(|skew|, a) ≈ 0`). **The paired jackknife
(27 fixed sub-cubes, cosmic variance cancels in the epoch-to-epoch difference, `jackknife.py`)
overturns that.** Detection first: box-mean copula skew is **13σ (R8) / 22σ (R4) below the
surrogate** per snapshot in cosmic-variance units, all 26 same sign — the detection is now
cosmic-variance-controlled, not just surrogate-controlled. Epoch structure: `|copula-skew|`
**rises from high z and knees at the DE epoch** —
- DE-band (z 0.44–0.66) − high-z (z 2–3): **Δ|skew| = +0.12, +5–7σ paired, 23–25/27 sub-cubes** ✓
- DE-band − low-z (z<0.16): Δ|skew| = +0.02–0.03, only +1.6–2σ, 17–18/27 (marginal)

So the curve is a real **rise-and-plateau**: `|skew|` climbs from ~0.41 (z≈2.5) to ~0.53
(z≈0.4–0.9), then mildly declines to ~0.50 (z=0). The knee sits on the count-peak (k, z≈0.70)
and S-peak (z≈0.59). This is a genuine, ≥5σ interior structure — I was wrong to call it flat.

## Result 3 — but the DE-epoch knee is the structure-formation clock, not a DE source

The honest interpretation of the knee: the copula Third is generated by nonlinear collapse, so
it grows with structure and **saturates when growth saturates — which happens at acceleration
onset because DE suppresses growth**. The coincidence of the knee with the DE epoch is therefore
the *expected consequence* of DE turning on, not independent evidence that the Third *sources*
DE. It shares the same clock as the pairwise `S` (both sourced by the same collapse; both knee
at the count-peak), which is a consistency, not a new degree of freedom. The DM-as-Thirdness
prong is **not** rescued — but "the blind spot has a real structure-formation history that
tracks the ledger's own clock" is a stronger and truer statement than "flat."

## What was measured, stated plainly

- **The ledger has a real, occupied blind spot.** At small scales the halo field carries genuine
  amplitude-blind three-way coordination `S` cannot see (~20% of the 3-point, 4–6σ/snapshot at
  R=8 against a pairwise-matched null, unanimous sign). Peirce's Third is not a metaphor here — it
  is a measured ~0.35-in-standardized-skewness object.
- **It is not the dark sector and not a backward-boundary carrier.** Flat in time, scale-localized
  to the nonlinear regime, no DE-epoch onset. The DM/backward-ω prongs are unchanged.
- **It sharpens the keeper (grain=Third).** The un-booked Third concentrates at exactly the small,
  nonlinear scales where the "which-units-post" grain choice bites hardest — the ledger's blind
  spot and the grain problem live in the same place.

## Caveats (honest limits; none touch the detection)

- **R=4 is near the NGP grid scale (cell ≈ 3.2 Mpc/h)** — aliasing could differ between clustered
  real and phase-random surrogate fields, so treat the R=4 *magnitude* cautiously. **R=8 is the
  clean detection** (well above the cell scale, still 26/26, mean z −1.6, min −3.0).
- **Single box** — the DETECTION is surrogate-controlled and robust; the EPOCH STRUCTURE (the mild
  z≈0.5 bump) needs sub-volume jackknife errors before it can be called real. That is the one clean
  next step, plus CIC (anti-alias) assignment and implementing the copula-bispectrum cross-check
  (estimator #3, specified but not yet run).
- Halo tracers, not the full matter field (appropriate — it is the DE-book grain — but biased).

**Verdict:** the blind spot is real and occupied (~20%, small-scale, robust), the program's
"higher-order is empty/off-books" expectation is falsified at small scales, and the signal is
NOT a dark-sector component (flat history). A sliver built into the whole: the Third exists, it
is measured, and it lives where the grain problem lives — not where dark energy is booked.
