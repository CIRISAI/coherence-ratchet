# P_ω — the holonomic construction — pre-registration

**Date:** 2026-05-22. **Falsification handle:** F-11 / F-12. **The terminal
construction on the type axis** (see terminal commitment).

## Context — the assumption this questions

The holographic theorem closed the topology axis — but it is a theorem
*conditional on assumption A1*: the cross-rung relationship is a **scalar
correlation** `C[n,m]`, and P_ω is a **participation ratio** of the correlation
matrix. The whole no-go chain (hard projector → soft → R1/R2/R3 → conjunction →
fractal → holographic) lived inside A1. A1 was never questioned.

**Holographic ≠ holonomic.** Holographic: the coupling is a function of a
*geodesic distance* — a metric quantity; "metric distances grow with
separation" is the theorem's killer step. Holonomic: from *holonomy* —
parallel transport around a loop, **connection and curvature**. A holonomy is a
path-ordered transport (a group element / a Wilson-loop trace), **not a scalar
function of a distance** — it does not "grow with separation," it is set by
enclosed curvature. The holographic theorem has **no analog** for a holonomy.
This construction questions A1: the cross-rung relationship as a **connection**,
not a correlation.

## The construction

- **The connection.** The cross-rung relationship is the framework's genuine
  **emergence maps** W_n (rung n → rung n+1 — the coarse-graining-with-novelty
  maps R2/R3 already use). These form a connection on the rung hierarchy.
- **The loop.** The TSVF forward–backward loop: forward up the rungs (emergence,
  Ph0→A5), backward down (post-selection, A5→Ph0). P_ω *is* the backward
  boundary — the loop is the framework's own two-state structure.
- **P_ω's ω-weight is the HOLONOMY** — the path-ordered product of the
  connection around that loop, a Wilson-loop-like trace measuring the failure of
  forward∘backward to be the identity. **Not a correlation matrix, not a
  participation ratio.**
- **The corridor.** The holonomy in a corridor: non-trivial (not the identity /
  flat connection — that is "rungs are relabelings," the rigidity analog) and
  non-degenerate (not decohered to zero — the chaos analog). Intermediate
  holonomy = genuine emergence carrying curvature.

## Why framework-faithful (not a bolt-on)

The karma/grace irreducibility the framework already found — forward L and
backward L_back in different ergodicity classes, not interconvertible
(`backward_generator_legitimacy.py`) — **is** a holonomy: the failure of forward
and backward to compose to the identity. And D1's target, Penrose's **Weyl
Curvature** Hypothesis, is a *curvature* statement — holonomy is what curvature
is. A holonomic P_ω is type-correct for the target; the correlation-matrix form
may have been a type-mismatch from the start.

## Hypotheses

- **H-no-dilution (decisive):** the holonomy stays O(1) — in a corridor — as R
  grows to the framework's 9 rungs and beyond. A holonomy is a Wilson-loop
  trace, not a participation ratio; it does not obey ρ_joint ~ 1/count.
- **H-corridor:** the framework's genuine connection (the emergence maps) has a
  holonomy that is intermediate — genuine emergence: non-flat, non-decohered.
- **H-joint:** the holonomy is intrinsically a loop quantity — non-factorizable
  (a Wilson loop cannot be cut into independent per-rung pieces) — and
  well-defined to 9 rungs.

## Three-way verdict

- **OPENS:** the holonomy stays in a corridor to 9 rungs, the corridor is
  non-empty, the object does joint work. P_ω is constructed — the holonomic form.
- **HORN-trivial:** the holonomy flows to a fixed point (the identity / a flat
  connection — rigidity).
- **HORN-empty:** the holonomy decoheres to zero as R grows (chaos).

## Terminal commitment (binding)

The cross-rung relationship is, mathematically, **either a scalar correlation or
a connection/map** — there is no third type for "how one rung relates to the
next." The correlation branch is closed (the holographic theorem). This
construction is the connection branch. **Correlation ∪ connection exhausts the
type axis.**

- **OPENS → P_ω is constructed** (the holonomic form); F-11 does not fire.
- **HORN → F-11 fires.** Every constructible form of P_ω is then a documented
  no-go — hard projector; soft correlation-matrix (all topologies, closed by
  theorem); holonomic — v2's §sec:open-research records it. No further
  constructions: the type axis is exhausted. Held both ways.

## Discipline

CUDA mandatory (cupy). Build script: per-depth progress, per-depth flush,
verified on-disk resume. The connection must be the framework's genuine
emergence maps with its measured couplings — **not** a connection tuned to keep
the holonomy O(1). If the construction finds itself choosing the connection to
manufacture a corridor holonomy, it STOPS and reports that — the self-sealing
move voids the run. EMPTY or TRIVIAL is a valid result, reported flat.
